---
phase: 02-core-measurement-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/analysis/error_propagation.py]
autonomous: true
requirements: [MEAS-01, MEAS-02, MEAS-03, VAL-03]

must_haves:
  truths:
    - "ErrorPropagationTracker registers forward hooks at 6 per-layer points (P0-P6) plus 3 global points (G0-G2) on all 12 transformer layers without modifying transformer.py"
    - "A single FP16 forward pass saves all pre-activation tensors x for offline per-matrix computation"
    - "For any quantized weight matrix with round-to-nearest, the pipeline computes ||(W_q - W)x||/||Wx|| from saved activations"
    - "Null measurement (W_q = W_fp) produces ||dy||/||y|| < 1e-5 for every matrix"
  artifacts:
    - path: "src/analysis/error_propagation.py"
      provides: "ErrorPropagationTracker class with hook-based activation capture"
      min_lines: 200
      exports: ["ErrorPropagationTracker"]
  key_links:
    - from: "ErrorPropagationTracker.attach()"
      to: "model.named_modules() Linear layers"
      via: "register_forward_pre_hook"
      pattern: "isinstance.*nn\.Linear.*register_forward_pre_hook"
    - from: "ErrorPropagationTracker._register_p_point_hooks()"
      to: "TransformerLayer submodules (input_norm, attention, post_attn_norm, ffn)"
      via: "register_forward_hook and register_forward_pre_hook"
      pattern: "layer\.(input_norm|attention|post_attn_norm|ffn)\.register_forward"
    - from: "ErrorPropagationTracker.compute_output_error()"
      to: "FPQuantizer.quantize()"
      via: "quantizer.quantize(W_fp)"
      pattern: "quantizer\.quantize\(W_fp\)"
    - from: "ErrorPropagationTracker.activations"
      to: "compute_output_error result dict"
      via: "module_path key mapping (no .weight suffix)"
      pattern: "results\[module_path\]"
---

<objective>
Implement `ErrorPropagationTracker` -- a hook-based measurement class in `src/analysis/error_propagation.py` that captures per-Linear-module pre-activation tensors x from a single FP16 forward pass and computes output-space relative error ||(W_q - W)x|| / ||Wx|| offline for all linear layers.

Purpose: Provide the core measurement infrastructure that Phase 3 (Theorem 1 Validation) and Phase 4 (Error Propagation Trace) depend on. The tracker is external to transformer.py -- all instrumentation via PyTorch forward hooks.

Output: `src/analysis/error_propagation.py` with a complete `ErrorPropagationTracker` class supporting attach/detach, activation capture, P-point/G-point measurement, offline error computation, and null measurement validation.
</objective>

<execution_context>
@/home/rimuru/.claude/get-shit-done/workflows/execute-plan.md
@/home/rimuru/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-measurement-protocol/02-CONTEXT.md
@.planning/phases/02-core-measurement-protocol/02-RESEARCH.md

<interfaces>
Relevant exports and patterns from existing code that the executor needs.

From src/quantization/gptq.py (hook pattern reference -- lines 193-255):
- `_collect_activations()` registers `register_forward_hook` on all nn.Linear modules
- Uses factory function `make_hook(name)` to avoid closure capture bugs
- `inp[0].detach()` for activation capture, stores in dict
- Hook handles stored in list for cleanup via `.remove()`

From src/quantization/fp_quantizer.py (quantizer API -- lines 53-122):
- `FPQuantizer(fmt='fp4_e2m1', per_channel=True)` constructor
- `FPQuantizer.quantize(x, stochastic=False, scale=None)` -- returns quantized tensor
- `_simulate_quantize()`: scales by amax, rounds to nearest grid point, descales
- Grid: 16-level FP4 E2M1 grid (`FP4_LEVELS`)

From src/model/transformer.py (model structure for hook placement):
- `TransformerLayer.forward()` lines 181-194: residual=hidden_states [P0] → input_norm(hidden_states) [P1] → attention [P2] → residual+hidden_states [P3] → post_attn_norm [P4] → ffn [P5] → residual+hidden_states [P6]
- `MicroGemmaFPModel.forward()` lines 215-226: embed_tokens [G0] → layers loop → norm [G1]
- `MicroGemmaFPForCausalLM.forward()` lines 245-257: model → lm_head [G2]
- `get_quantizable_weights()` lines 264-270: returns list of (name, param) for dim>=2 params with 'proj'/'embed_tokens'/'lm_head' in name
- `lm_head.weight = model.embed_tokens.weight` (line 235, tied weights)
- embed_tokens is nn.Embedding (not nn.Linear), lm_head is nn.Linear

From src/model/config.py:
- `MicroGemmaFPConfig()`: hidden_size=768, intermediate_size=3072, num_hidden_layers=12, hidden_size_per_layer_input=64, vocab_size=32000, max_seq_length=512

From src/analysis/condition.py (conventions reference):
- Module-level docstring, section-header comments with box separators
- Functions use @torch.no_grad() where appropriate
- Results returned as dict[str, float]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ErrorPropagationTracker with Linear pre-hook registration</name>
  <files>src/analysis/error_propagation.py</files>
  <read_first>
    - src/quantization/gptq.py:193-255 (existing hook pattern for activation capture -- _collect_activations, make_hook, handle management, detach pattern)
    - src/model/transformer.py:264-270 (get_quantizable_weights -- module naming convention)
    - src/analysis/condition.py:1-10 (analysis module conventions: docstring style, import pattern, section-header comments)
  </read_first>
  <behavior>
    - Test 1: ErrorPropagationTracker.__init__ initializes empty _activations dict, _hook_handles list, _activation_keys list
    - Test 2: attach() iterates model.named_modules(), registers pre-hook on each nn.Linear (isinstance check), stores handles
    - Test 3: Pre-hook callback captures input_args[0], detaches, clones, moves to CPU, stores in _activations[module_path]
    - Test 4: Pre-hook uses factory function pattern (def _make_input_hook) to avoid closure-capture bug
    - Test 5: detach() calls handle.remove() on all stored handles, clears handle list, prints count
    - Test 6: activations property returns a copy of the internal dict (not the reference)
  </behavior>
  <action>
    Create src/analysis/error_propagation.py implementing the ErrorPropagationTracker class skeleton with:

    Module docstring: "Hook-based activation capture and offline per-matrix output-space relative error computation for quantized Transformer models."

    Class ErrorPropagationTracker with:
    - __init__: Initialize empty self._activations (dict), self._p_points (dict), self._g_points (dict), self._hook_handles (list), self._activation_keys (list). Docstring explains the three storage dicts.
    - attach(model): Calls _register_linear_pre_hooks(model). Prints registration summary: "[Tracker] Registered {N} Linear pre-hooks". Returns self.
    - _register_linear_pre_hooks(model): Iterate model.named_modules(). For each (name, module) where isinstance(module, nn.Linear): store module_path = name (dot-separated, e.g. 'model.layers.0.attention.q_proj'), register forward_pre_hook via _make_input_hook(module_path), append handle to self._hook_handles, append module_path to self._activation_keys (preserves insertion order).
    - _make_input_hook(module_path: str): Returns a @torch.no_grad() closure (factory function pattern -- NOT a lambda). The closure captures module_path by value. Inside: x = input_args[0].detach().clone().cpu(), store in self._activations[module_path] = x.
    - detach(): Iterate self._hook_handles, call handle.remove() on each. Clear self._hook_handles. Print "[Tracker] Removed {N} hooks". Returns self.
    - activations property: Returns dict(self._activations) -- a copy, not the original reference.

    Do NOT register P-point or G-point hooks in this task -- those are Task 2.

    Use the existing codebase conventions: absolute imports from src.*, 4-space indent, box-separator section comments, @torch.no_grad() decorators. No modification to transformer.py, gptq.py, or any other file.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create tests/test_error_propagation_tracker.py first with unit tests for __init__, attach, pre-hook factory pattern test with a mock nn.Linear module, detach handle removal, and activations property copy behavior</automated>
  </verify>
  <acceptance_criteria>
    - src/analysis/error_propagation.py exists with module docstring
    - ErrorPropagationTracker class defined with __init__, attach, detach, activations
    - _register_linear_pre_hooks uses isinstance(module, nn.Linear) check
    - _make_input_hook is a def factory function (not lambda), capturing module_path as local parameter
    - Pre-hook callback: input_args[0].detach().clone().cpu() stored in self._activations[module_path]
    - detach() calls .remove() on every handle in self._hook_handles, then clears list
    - activations property returns dict(self._activations) copy
    - `python -m py_compile src/analysis/error_propagation.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add P-point and G-point measurement hook registration</name>
  <files>src/analysis/error_propagation.py</files>
  <read_first>
    - src/analysis/error_propagation.py (class from Task 1 -- current state)
    - src/model/transformer.py:172-194 (TransformerLayer.forward -- exact measurement point locations for P0-P6: P0=input to layer at line 183, P1=after input_norm at line 184, P2=after attention at line 185, P3=residual+hidden_states at line 186, P4=after post_attn_norm at line 190, P5=after ffn at line 191, P6=residual+hidden_states at line 192)
    - src/model/transformer.py:215-226 (MicroGemmaFPModel.forward -- G0 after embed_tokens line 220, G1 after norm line 225)
    - src/model/transformer.py:245-257 (MicroGemmaFPForCausalLM.forward -- G2 after lm_head line 247)
  </read_first>
  <behavior>
    - Test 1: _register_p_point_hooks registers 5 hooks per layer (P0 pre-hook, P1/P2/P4/P5 forward hooks) across 12 layers = 60 hooks
    - Test 2: P0 hook captures layer input, P1 captures input_norm output, P2 captures attention output, P4 captures post_attn_norm output, P5 captures FFN output
    - Test 3: _register_g_point_hooks registers 3 hooks (G0 on embed_tokens, G1 on norm, G2 on lm_head)
    - Test 4: compute_p3_p6 computes P3 = P0 + P2 and P6 = P3 + P5 for each layer with available P0/P2/P5
    - Test 5: All captured tensors are detached, cloned, moved to CPU
    - Test 6: p_points property returns merged dict of _p_points and _g_points
  </behavior>
  <action>
    Extend ErrorPropagationTracker (in the existing src/analysis/error_propagation.py) with P-point and G-point measurement hooks:

    Update attach(model) to also call _register_p_point_hooks(model) then _register_g_point_hooks(model). Update print summary: "[Tracker] Registered {N} Linear pre-hooks, {M} P-point hooks, {K} G-point hooks".

    Add _register_p_point_hooks(model):
    - Access layers: model.model.layers. If that fails (AttributeError), try getattr(model, 'layers', []). Iterate with enumerate for layer_idx.
    - P0: register_forward_pre_hook on the layer itself (TransformerLayer) via _make_p_hook(layer_idx, 'P0'). Pre-hook captures input_args[0] to the layer's forward method.
    - P1: register_forward_hook on layer.input_norm via _make_p_hook(layer_idx, 'P1'). Forward hook captures the module output.
    - P2: register_forward_hook on layer.attention via _make_p_hook(layer_idx, 'P2')
    - P4: register_forward_hook on layer.post_attn_norm via _make_p_hook(layer_idx, 'P4')
    - P5: register_forward_hook on layer.ffn via _make_p_hook(layer_idx, 'P5')
    - Store all handles in self._hook_handles.
    - P3 and P6 are NOT hooked -- they are computed in compute_p3_p6().

    Add _make_p_hook(layer_idx: int, point_id: str):
    - Returns a @torch.no_grad() closure. The closure function signature is hook(module, input_args, output=None).
    - If output is not None (forward hook): tensor = output.detach().clone().cpu()
    - Else (pre-hook for P0): tensor = input_args[0].detach().clone().cpu()
    - Store as self._p_points[f"{layer_idx}_{point_id}"] = tensor

    Add _register_g_point_hooks(model):
    - G0: register_forward_hook on model.model.embed_tokens via _make_g_hook('G0')
    - G1: register_forward_hook on model.model.norm via _make_g_hook('G1')
    - G2: register_forward_hook on model.lm_head via _make_g_hook('G2')
    - Store handles in self._hook_handles.

    Add _make_g_hook(point_id: str):
    - Returns a @torch.no_grad() closure hook(module, input_args, output).
    - tensor = output.detach().clone().cpu(). If isinstance(tensor, tuple), take tensor[0] (handles the case where lm_head returns (logits,) tuple or similar).
    - Store as self._g_points[point_id] = tensor

    Add compute_p3_p6():
    - Find all P0 keys in self._p_points (keys ending in '_P0'). For each:
      - Extract layer_prefix by removing '_P0' suffix
      - If f"{layer_prefix}_P2" in self._p_points: compute P3 = P0 + P2, store as self._p_points[f"{layer_prefix}_P3"] = (p0 + p2).clone()
      - If f"{layer_prefix}_P3" in self._p_points and f"{layer_prefix}_P5" in self._p_points: compute P6 = P3 + P5, store as self._p_points[f"{layer_prefix}_P6"] = (p3 + p5).clone()

    Add p_points property: return {**self._p_points, **self._g_points}

    Do NOT add compute_output_error or validate_null_measurement -- those are Task 3.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create tests/test_error_propagation_tracker.py with tests: mock model with 2 layers having input_norm/attention/post_attn_norm/ffn submodules, verify 5 hooks per layer registered, verify P-point keys follow "{layer_idx}_{point_id}" format, verify compute_p3_p6 produces correct residual sums, verify G-point hooks registered on model.model.embed_tokens/norm and model.lm_head</automated>
  </verify>
  <acceptance_criteria>
    - _register_p_point_hooks registers pre_hook on layer (P0) and forward_hook on layer.input_norm (P1), layer.attention (P2), layer.post_attn_norm (P4), layer.ffn (P5) for all 12 layers
    - P0 pre-hook captures input_args[0]; P1/P2/P4/P5 forward hooks capture output
    - _register_g_point_hooks registers forward_hook on model.model.embed_tokens (G0), model.model.norm (G1), model.lm_head (G2)
    - _make_g_hook handles tuple output (isinstance check, takes tensor[0])
    - compute_p3_p6: P3 = P0 + P2, P6 = P3 + P5 for each layer where source tensors exist
    - All captured tensors: detach().clone().cpu()
    - p_points property returns merged {**_p_points, **_g_points}
    - attach() prints summary with Linear pre-hook count, P-point count, G-point count
    - `python -m py_compile src/analysis/error_propagation.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add offline error computation and null measurement validation</name>
  <files>src/analysis/error_propagation.py</files>
  <read_first>
    - src/analysis/error_propagation.py (class from Tasks 1-2 -- current state with all hook registration)
    - src/quantization/fp_quantizer.py:79-122 (FPQuantizer.quantize() API: accepts tensor, stochastic=False, scale=None; returns quantized tensor via _simulate_quantize or native dtype)
    - src/model/transformer.py:264-270 (get_quantizable_weights naming: 'model.layers.0.attention.q_proj.weight' -- strip '.weight' for module path)
    - src/analysis/condition.py:55-65 (compute_all_condition_numbers pattern: iterates model.named_parameters(), checks dim()>=2, returns dict[name->float])
  </read_first>
  <behavior>
    - Test 1: _resolve_module('model.layers.0.attention.q_proj') traverses model attributes and returns the nn.Linear module
    - Test 2: _resolve_module returns None for nonexistent path
    - Test 3: compute_output_error returns dict[str, float] with module_path keys (no .weight suffix)
    - Test 4: Error formula uses Frobenius norm: (y_q - y_fp).norm() / y_fp.norm().clamp(min=1e-12)
    - Test 5: With mock quantizer that returns W_fp unchanged, all errors are 0.0 (or negligibly small from FP arithmetic)
    - Test 6: validate_null_measurement raises ValueError when any module exceeds 1e-5 threshold
    - Test 7: validate_null_measurement returns max error as float when all modules pass
  </behavior>
  <action>
    Extend ErrorPropagationTracker (in the existing src/analysis/error_propagation.py) with error computation and null measurement:

    Add _resolve_module(model, module_path: str):
    - Split module_path by '.' into parts list.
    - obj = model. For each part in parts: if hasattr(obj, part): obj = getattr(obj, part); else return None.
    - Return obj (the resolved nn.Module or None).

    Add compute_output_error(model, quantizer) -> dict[str, float]:
    - Decorate with @torch.no_grad().
    - Initialize empty results dict.
    - Iterate self._activations items: (module_path, x_cpu).
    - For each: module = self._resolve_module(model, module_path). If module is None or not hasattr(module, 'weight'): continue (skip).
    - W_fp = module.weight.data (tensor on model's device).
    - device = W_fp.device; x = x_cpu.to(device).
    - y_fp = x @ W_fp.T (matrix multiply: (batch, seq, in_features) @ (in_features, out_features).T = (batch, seq, out_features)).
    - W_q = quantizer.quantize(W_fp) (deterministic round-to-nearest per D-07).
    - y_q = x @ W_q.T.
    - denom = y_fp.norm().clamp(min=1e-12); err = (y_q - y_fp).norm() / denom.
    - results[module_path] = err.item().
    - Return results.

    Add validate_null_measurement(model) -> float:
    - Decorate with @torch.no_grad().
    - Initialize max_err = 0.0, violations = [].
    - Iterate self._activations items: (module_path, x_cpu).
    - Resolve module, skip if None or no weight.
    - W_fp = module.weight.data; x = x_cpu.to(W_fp.device).
    - y_fp = x @ W_fp.T; y_q = x @ W_fp.T (identical -- identity quantization, W_q == W_fp).
    - denom = y_fp.norm().clamp(min=1e-12); err = (y_q - y_fp).norm() / denom.
    - err_val = err.item(); max_err = max(max_err, err_val).
    - If err_val > 1e-5: violations.append((module_path, err_val)).
    - If violations: print "[WARN] Null measurement failed: {len(violations)} modules exceed 1e-5. Max error: {max_err:.2e}". Print up to 5 violating paths with values. raise ValueError with the same message.
    - Return max_err.

    Both methods must handle the case where the tensor x has been moved to CPU by the hooks and the weight lives on GPU -- the `x = x_cpu.to(device)` step handles this.

    The quantizer for error computation is passed as a parameter, not hardcoded -- callers pass FPQuantizer(fmt='fp4_e2m1', per_channel=True).

    Do NOT modify transformer.py or any other file.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create tests/test_error_propagation_tracker.py with tests: _resolve_module traverses nested attributes correctly, _resolve_module returns None on bad path, compute_output_error with identity quantizer produces 0.0 errors for a mock module, validate_null_measurement raises ValueError when error > 1e-5, validate_null_measurement returns float when all pass</automated>
  </verify>
  <acceptance_criteria>
    - _resolve_module('model.layers.0.attention.q_proj') correctly traverses nested attributes
    - _resolve_module returns None for nonexistent paths
    - compute_output_error returns dict[str, float] keyed by module_path (no .weight suffix on keys)
    - Error formula: (y_q - y_fp).norm() / y_fp.norm().clamp(min=1e-12) -- Frobenius norm ratio
    - x moved to W_fp.device before computation (handles CPU-stored activations vs GPU weights)
    - y_fp = x @ W_fp.T, y_q = x @ W_q.T preserving full (batch, seq, out_features) shape
    - validate_null_measurement uses W_q = W_fp (no quantization call, identity)
    - validate_null_measurement raises ValueError with [WARN] message when any err > 1e-5
    - validate_null_measurement prints up to 5 violating module paths with error values
    - Both methods decorated with @torch.no_grad()
    - `python -m py_compile src/analysis/error_propagation.py` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | ErrorPropagationTracker is an internal measurement tool with no external input surfaces, no network access, no user data exposure, and no persistence layer. All hooks operate on pre-existing model tensors in-memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | error_propagation.py (activations storage) | accept | Activation tensors stored in CPU RAM are model-internal data; no PII, no persistence to disk, no network exposure. Accept as non-threat. |
| T-02-02 | Denial of Service | error_propagation.py (hook accumulation) | mitigate | Undetached tensors in hooks keep autograd graph alive causing GPU OOM. Mitigated by mandatory .detach().clone().cpu() pattern in every hook callback per D-04/D-05. |
| T-02-03 | Tampering | error_propagation.py (hook handle leakage) | mitigate | Hooks not cleaned up corrupt subsequent forward passes. Mitigated by detach() calling .remove() on all stored handles and clearing handle list. |
| T-02-SC | Tampering | pip installs | accept | No new packages installed by this phase. Pure PyTorch + existing project code. |
</threat_model>

<verification>
After all 3 tasks complete:
- `python -m py_compile src/analysis/error_propagation.py` exits 0
- Class has all public methods: attach, detach, compute_output_error, validate_null_measurement, compute_p3_p6
- Class has properties: activations, p_points
- All 12 locked decisions (D-01 through D-12) traceable to implementation
</verification>

<success_criteria>
A Python module `src/analysis/error_propagation.py` containing `ErrorPropagationTracker` that:
1. Registers forward pre-hooks on all nn.Linear modules without modifying transformer.py
2. Captures pre-activation tensors x on a single forward pass (detached, cloned, CPU-stored)
3. Registers measurement points P0-P6 (per-layer) and G0-G2 (global) with correct hook placement
4. Computes ||(W_q - W)x|| / ||Wx|| offline via compute_output_error(model, quantizer)
5. Validates the pipeline via validate_null_measurement (identity quant → error < 1e-5)
</success_criteria>

<output>
Create `.planning/phases/02-core-measurement-protocol/02-01-SUMMARY.md` when done
</output>
