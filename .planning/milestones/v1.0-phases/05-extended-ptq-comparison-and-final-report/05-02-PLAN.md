---
phase: 05-extended-ptq-comparison-and-final-report
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/experiments/write_final_report.py
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03

must_haves:
  truths:
    - "REPORT.md contains per-matrix summary table with all 9 columns for 72 matrices"
    - "REPORT.md contains error propagation waterfall data sourced from Phase 4 trace"
    - "REPORT.md updated with corrected kappa values, per-matrix measurements, and revised theoretical assessment"
    - "REPORT.md follows the 10-section structure defined in CONTEXT.md"
  artifacts:
    - path: "src/experiments/write_final_report.py"
      provides: "Automated report generation from all Phase 3/4/5 JSON results"
      min_lines: 150
    - path: "docs/REPORT.md"
      provides: "Final project report with corrected methodology and all numerical findings"
      contains: ["Executive Summary", "Methodology", "Theorem 1 Validation", "Error Propagation Trace", "Extended PTQ Comparison", "GPTQ Analysis", "Lloyd-Max Analysis", "RMSNorm Error Blocking", "Revised Theoretical Assessment", "References"]
  key_links:
    - from: "src/experiments/write_final_report.py"
      to: "results/theorem1_validation.json"
      via: "json.load"
      pattern: "theorem1_validation\\.json"
    - from: "src/experiments/write_final_report.py"
      to: "results/error_propagation_trace.json"
      via: "json.load"
      pattern: "error_propagation_trace\\.json"
    - from: "src/experiments/write_final_report.py"
      to: "results/full_comparison.json"
      via: "json.load"
      pattern: "full_comparison\\.json"
    - from: "src/experiments/write_final_report.py"
      to: "docs/REPORT.md"
      via: "file write"
      pattern: "docs/REPORT\\.md"
---

<objective>
Implement `src/experiments/write_final_report.py` — a standalone report generation script that reads all Phase 3, 4, and 5 JSON results and regenerates `docs/REPORT.md` with corrected methodology, per-matrix measurements, propagation waterfall data, and a revised theoretical assessment.

Purpose: Guarantee consistency between numerical results (JSON) and the written report. No manual copy-paste of numerical values — every number in REPORT.md is sourced programmatically from JSON data.

Output: Regenerated `docs/REPORT.md` following the 10-section structure from CONTEXT.md.
</objective>

<execution_context>
@/home/rimuru/.claude/get-shit-done/workflows/execute-plan.md
@/home/rimuru/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-extended-ptq-comparison-and-final-report/05-CONTEXT.md:11-72 (locked decisions D-01 through D-14), 157-168 (REPORT.md 10-section structure)
@docs/ANALYSIS.md (mathematical derivations of Theorems 1-4, reference only — do not modify)

<interfaces>
Key data structures the executor reads from JSON files and the expected structure of REPORT.md output.

From results/theorem1_validation.json:
```
{
  "checkpoint": str,
  "num_matrices": int,
  "bonferroni_alpha": float,
  "pearson_r": float,
  "pearson_p": str,
  "bootstrap_ci": [float, float],
  "seed_by_seed_r": [float, float, float],
  "subgroup_correlations": {str: {r: float, p: float}},
  "verdict": "YES"|"QUALIFIED"|"NO",
  "verdict_reason": str,
  "results": [
    {
      "name": "model.layers.0.attention.q_proj",
      "layer": int,
      "type": "attention"|"ffn"|"global",
      "kappa": float,
      "dw_norm": float,
      "dy_norm_mean": float,
      "dy_norm_std": float,
      "tightness_ratio": float
    }
  ]
}
```

From results/error_propagation_trace.json:
```
{
  "waterfall": dict,           // per-source P-point error sequences
  "norm_attenuation": {        // per-layer RMSNorm attenuation
    str: {                     // layer key e.g. "0", "1", ..., "11"
      "input_norm": float,     // ||delta_post|| / ||delta_pre|| at input_norm
      "post_attn_norm": float, // ||delta_post|| / ||delta_pre|| at post_attn_norm
      "parallel_component": float,
      "orthogonal_component": float
    }
  },
  "metadata": {}
}
```

From results/full_comparison.json (generated by Plan 01):
```
{
  "configs": {config_key: {ppl: float, per_matrix_errors: {str: float}}},
  "comparisons": {
    "gptq_vs_rtn": {key: {ppl_delta, mean_dy_delta, per_matrix_delta}},
    "lloyd_max_vs_uniform": {key: {ppl_delta, mean_dy_delta, per_matrix_delta}}
  },
  "per_matrix_summary": [{name, layer, type, kappa, dw_norm, dy_norm, tightness_ratio, norm_attenuation}],
  "metadata": {}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write write_final_report.py — argparse, JSON loading, per-matrix summary table formatting, methodology section</name>
  <read_first>
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/.planning/phases/05-extended-ptq-comparison-and-final-report/05-CONTEXT.md:157-168 (REPORT.md sections)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/docs/REPORT.md (existing report — understand current structure, identify what gets replaced vs preserved)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/docs/ANALYSIS.md (mathematical derivations for Theorems 1-4 — reference only, do not modify)
  </read_first>
  <files>src/experiments/write_final_report.py</files>
  <action>
    Create `src/experiments/write_final_report.py` as a new file. This is a CPU-only script (no GPU, no PyTorch imports needed except for JSON data reading).

    **Argparse interface (per D-12):**
    - `--results_dir` (default `results`) — directory containing all JSON result files
    - `--output` (default `docs/REPORT.md`) — output Markdown path
    - `--analysis_doc` (default `docs/ANALYSIS.md`) — path to mathematical derivations to reference (read-only, do not modify)

    **JSON loading and validation:**

    Function `load_json(path)`:
    - Reads JSON file at path
    - Returns parsed dict
    - If file missing: `sys.exit(1)` with clear error listing the missing path
    - All three JSON files must be present: `theorem1_validation.json`, `error_propagation_trace.json`, `full_comparison.json`

    **Per-matrix summary table formatting (REPORT-01):**

    Function `format_summary_table(per_matrix_data)`:
    - Input: sorted list of 72 dicts from `full_comparison.json['per_matrix_summary']`
    - Output: Markdown-formatted table string with columns: `Matrix`, `Layer`, `Type`, `κ(W)`, `||δW||/||W||`, `||δy||/||y||`, `Tightness`, `RMSNorm Atten.`
    - Use 4 decimal places for all floats; render NaN/Inf as `—`
    - Section header: `## Per-Matrix Error Summary` with a 1-2 sentence description of what the table shows
    - Also print a summary row: mean ||dy||/||y|| across all 72 matrices

    **Methodology section generation (REPORT-03):**

    Function `write_methodology_section()`:
    - Section header: `## Methodology (Corrected)`
    - Document the following methodology corrections relative to the original proposal:
      a. **Condition number computation:** Exact SVD via `torch.linalg.svdvals` (not power iteration approximation) — the original proposal overestimated κ values
      b. **Per-matrix measurement granularity:** ||dy||/||y|| computed at each Linear layer output (not after cascading through RMSNorm/attention/FFN) — this is the correct granularity for Theorem 1 testing
      c. **Clean data split:** Calibration on training split only, evaluation on validation split only — eliminates in-sample PPL optimism
      d. **Bonferroni correction:** α = 0.05/72 = 0.00069 for 72-matrix multiple comparison — mandatory statistical rigor
      e. **Single-pass activation capture:** FP16 activations captured once per checkpoint before any quantization — avoids cascading measurement confound
    - Reference `docs/ANALYSIS.md` for the mathematical derivations of Theorems 1-4

    **Placeholder for Task 2:**
    At the bottom of the file, add comment: `# ── Task 2 placeholder: remaining REPORT.md sections ──`
  </action>
  <verify>
    <automated>python -m py_compile src/experiments/write_final_report.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile src/experiments/write_final_report.py` passes
    - argparse has `--results_dir`, `--output`, `--analysis_doc` arguments
    - `load_json()` exits with `sys.exit(1)` on missing files
    - `format_summary_table()` produces Markdown-formatted table with 8 columns (name stripped to shorthand for readability, or full name in code font)
    - `write_methodology_section()` documents all 5 methodology corrections listed above
    - All number formatting uses 4 decimal places; NaN/Inf rendered as `—` (em dash)
  </acceptance_criteria>
  <done>Script skeleton: JSON loading, per-matrix table formatting, and methodology section generation are implemented and syntactically valid. Task 2 adds the remaining 8 report sections.</done>
</task>

<task type="auto">
  <name>Task 2: Add all remaining REPORT.md sections — theorem results, PTQ comparison, GPTQ/Lloyd-Max analysis, error propagation, RMSNorm, theoretical assessment</name>
  <read_first>
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/src/experiments/write_final_report.py (Task 1 output)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/docs/ANALYSIS.md (Theorems 1-4 derivations — reference for revised theoretical assessment)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/.planning/phases/05-extended-ptq-comparison-and-final-report/05-CONTEXT.md:157-168 (REPORT.md section structure)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/results/theorem1_validation.json (actual Phase 3 data — for extracting verdict, r, p, CI)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/results/error_propagation_trace.json (actual Phase 4 data — for waterfall and attenuation)
    - /home/rimuru/Projects/Code/homework/Numerical_Analysis/proj/results/full_comparison.json (actual Phase 5 data — for PTQ comparison, GPTQ/Lloyd-Max analysis)
  </read_first>
  <files>src/experiments/write_final_report.py</files>
  <action>
    Add the remaining REPORT.md section generation functions and the `main()` orchestrator to `write_final_report.py`. Insert after the Task 1 placeholder comment.

    **Generate the complete REPORT.md by calling each section function in order. Each function takes the relevant JSON data as input and returns Markdown-formatted string.**

    **Section functions to implement:**

    1. `write_executive_summary(th1_data, trace_data, comp_data)`:
       - Heading: `# Executive Summary`
       - 3-4 paragraphs summarizing:
         a. What was studied (FP4/FP8 PTQ on 164M Gemma-style Transformer, 72 weight matrices)
         b. Key findings from Theorem 1 (verdict from `th1_data['verdict']` + `r` and `p` values embedded in prose — e.g., "Pearson r = 0.XX, p = X.Xe-X")
         c. Key findings from PTQ comparison (best FP4 method, best FP8 method, PPL deltas)
         d. Key findings from error propagation (RMSNorm attenuation, where error concentrates)
       - Embed concrete numbers directly in prose (no cross-references like "see Section X")

    2. `write_theorem1_results(th1_data)`:
       - Heading: `## Theorem 1 Validation Results`
       - Subsection `### Statistical Analysis`:
         - Pearson r, p-value, Bonferroni alpha = 0.00069
         - Bootstrap 95% CI
         - Verdict: YES/NO/QUALIFIED with full reason text (from `th1_data['verdict_reason']`)
       - Subsection `### Seed-by-Seed Correlations`:
         - Table: Seed | r | p-value (3 rows)
       - Subsection `### Per-Layer-Type Subgroup Analysis`:
         - Table: Type | n | r | p-value (attention, ffn, global)

    3. `write_propagation_trace(trace_data)`:
       - Heading: `## Error Propagation Trace` (REPORT-02)
       - Subsection `### RMSNorm Attenuation`:
         - Table with headers: Layer | Input Norm Attenuation | Post-Attn Norm Attenuation | ||δ_parallel|| | ||δ_orthogonal||
         - 12 rows (one per layer)
         - Interpretive text: does RMSNorm attenuate error? By how much (mean attenuation)? Is it parallel or orthogonal?
       - Subsection `### Error Waterfall (Representative Layers)`:
         - For layers 0, 5, 11: show P-point error waterfall data if available in trace_data['waterfall']
         - If waterfall data is structured per-source, present the most informative source (e.g., FP4 RTN on q_proj for each layer)

    4. `write_ptq_comparison(comp_data)`:
       - Heading: `## Extended PTQ Comparison`
       - Subsection `### 24-Config Comparison Matrix`:
         - Table: Checkpoint | Format | Method | PPL | ΔPPL | mean ||δy||/||y||
         - Rows grouped by checkpoint, then format, sorted by PPL ascending within group
         - Include FP16 baseline row for each checkpoint
       - Subsection `### Best Per-Checkpoint Summary`:
         - Text: best FP8 method, best FP4 method per checkpoint with PPL and mean ||dy||/||y||
       - Claude's discretion: if Hadamard/Outlier FP4 results are absent (skipped), note in prose that these were omitted due to expected FP4 instability

    5. `write_gptq_analysis(comp_data)`:
       - Heading: `## GPTQ Analysis: Column Compensation vs Output Error` (COMP-02)
       - For each GPTQ vs RTN pair in `comp_data['comparisons']['gptq_vs_rtn']`:
         - Subsection per pair: e.g., `### FP16 Baseline / FP8 E4M3`
         - Text: PPL delta, mean ||dy||/||y|| delta, interpretation ("GPTQ reduces output-space error by X%" or "GPTQ increases output-space error by X%")
         - If both FP8 and FP4 pairs exist, add a cross-format observation paragraph
       - Note per D-04: negative delta means GPTQ reduces error

    6. `write_lloyd_max_analysis(comp_data)`:
       - Heading: `## Lloyd-Max Analysis: Adaptive Grids vs Uniform E2M1` (COMP-03)
       - For each Lloyd-Max vs uniform pair in `comp_data['comparisons']['lloyd_max_vs_uniform']`:
         - Subsection per pair
         - Text: PPL delta, mean ||dy||/||y|| delta, interpretation
       - Note per D-06: negative delta means Lloyd-Max reduces error vs uniform

    7. `write_rmsnorm_analysis(trace_data, comp_data)`:
       - Heading: `## RMSNorm Error Blocking`
       - Synthesis of Phase 2 (null measurement validation), Phase 4 (attenuation ratios), and Phase 5 (per-matrix errors)
       - Quantitative claim: what fraction of per-matrix ||dy||/||y|| survives RMSNorm? (mean input_norm attenuation across all layers)
       - Does RMSNorm block error (reduce magnitude) or redirect it (change direction)? Answer with parallel vs orthogonal decomposition data

    8. `write_theoretical_assessment(th1_data, trace_data, comp_data)`:
       - Heading: `## Revised Theoretical Assessment`
       - Original hypothesis: Theorem 1 predicts ||dy||/||y|| <= κ(W) * ||dW||/||W|| as an upper bound
       - Revised understanding (based on `th1_data['verdict']`):
         - If YES: "The bound holds. κ is a statistically significant predictor..."
         - If QUALIFIED: "Partial support. κ correlates but the bound is loose/tight for specific matrix types. Specifically: ..."
         - If NO: "The bound does not hold empirically. Possible reasons: ..."
       - Discuss what κ misses: off-diagonal error coupling, cascading error through layers, RMSNorm's non-multiplicative effect
       - Reference specific evidence: correlation values, tightness_ratio distribution, RMSNorm attenuation
       - Reference ANALYSIS.md Theorems 1-4 as the mathematical foundation

    9. `write_references(analysis_doc_path)`:
       - Heading: `## References`
       - `1. ANALYSIS.md` — mathematical derivations for Theorems 1-4
       - `2. PROPOSAL.md` — original project proposal
       - `3. results/theorem1_validation.json`
       - `4. results/error_propagation_trace.json`
       - `5. results/full_comparison.json`
       - `6. results/per_matrix_summary.json`

    **main() function:**
    1. Parse args
    2. Load all three JSON files via `load_json()`
    3. Open `--output` file for writing
    4. Call each section function in order (1-9 above), write to file
    5. Print confirmation: "REPORT.md written to {output} ({N} sections, {M} bytes)"
    6. Return 0

    **Number formatting rules:**
    - PPL: 2 decimal places (e.g., `12.34`)
    - Perplexity deltas: 2 decimal places with sign (e.g., `+0.56`, `-0.23`)
    - ||dy||/||y||: 6 decimal places (e.g., `0.012345`)
    - κ: 2 decimal places for large values, scientific notation if > 1e6 (e.g., `1.23e7`)
    - Pearson r: 4 decimal places (e.g., `0.7234`)
    - p-values: scientific notation, 2 significant figures (e.g., `1.2e-05`)
    - NaN/Inf: render as `—` in tables, as "not available" in prose
  </action>
  <verify>
    <automated>python -m py_compile src/experiments/write_final_report.py && python -c "import ast; tree = ast.parse(open('src/experiments/write_final_report.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; required = ['load_json', 'format_summary_table', 'write_methodology_section', 'write_executive_summary', 'write_theorem1_results', 'write_propagation_trace', 'write_ptq_comparison', 'write_gptq_analysis', 'write_lloyd_max_analysis', 'write_rmsnorm_analysis', 'write_theoretical_assessment', 'write_references', 'main']; missing = [f for f in required if f not in funcs]; print(f'MISSING: {missing}' if missing else f'All {len(required)} required functions present')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile src/experiments/write_final_report.py` passes
    - All 13 required functions are defined: `load_json`, `format_summary_table`, `write_methodology_section`, `write_executive_summary`, `write_theorem1_results`, `write_propagation_trace`, `write_ptq_comparison`, `write_gptq_analysis`, `write_lloyd_max_analysis`, `write_rmsnorm_analysis`, `write_theoretical_assessment`, `write_references`, `main`
    - `main()` writes `docs/REPORT.md` with all 10 sections (Executive Summary through References)
    - Per-matrix summary table in REPORT.md has 8 columns with correctly formatted values
    - Theorem 1 verdict (YES/NO/QUALIFIED) and reason appear verbatim from `theorem1_validation.json`
    - GPTQ vs RTN analysis reports per-pair deltas with direction sign (negative = GPTQ better)
    - Lloyd-Max vs uniform analysis reports per-pair deltas with direction sign
    - RMSNorm analysis references parallel vs orthogonal decomposition from trace data
    - Revised theoretical assessment references specific evidence from all three phases
    - References section lists 6 items
    - All numerical values in REPORT.md are programmatically sourced from JSON — no hardcoded numbers
  </acceptance_criteria>
  <done>Complete report generation script: `python src/experiments/write_final_report.py --results_dir results --output docs/REPORT.md` regenerates the full final report. Every number in REPORT.md is sourced from JSON data — zero manual copy-paste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Script reads local JSON files and writes a local Markdown file. No network, auth, or external service operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | File write path | accept | REPORT.md is a course project document; no PII, credentials, or sensitive data involved. |
| T-05-04 | Denial of Service | Missing JSON files | mitigate | `load_json()` exits with `sys.exit(1)` and clear error message listing the missing path before any write occurs. No partial REPORT.md is generated. |
| T-05-SC | Tampering | pip installs | accept | No new packages installed. Standard library only (`json`, `argparse`, `os`, `sys`, `math`). |
</threat_model>

<verification>
  <automated>python -m py_compile src/experiments/write_final_report.py</automated>
</verification>

<success_criteria>
- `write_final_report.py` syntactically valid
- All 13 required functions defined
- `main()` reads 3 JSON files, generates `docs/REPORT.md` with 10 sections
- Per-matrix summary table programmatically sourced from JSON
- Theorem 1 verdict and evidence verbatim from Phase 3 JSON
- GPTQ and Lloyd-Max analyses with directional deltas
- Revised theoretical assessment with specific evidence references
- Report can be regenerated at any time by re-running the script (idempotent)
</success_criteria>

<output>
Create `.planning/phases/05-extended-ptq-comparison-and-final-report/05-02-SUMMARY.md` when done
</output>
